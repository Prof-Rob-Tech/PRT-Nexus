# Conversa PRT Nexus — 22 set 2026

Registro para continuar no outro PC. Abra este arquivo, copie o conteúdo e cole num chat novo do Cursor. O Cursor não recoloca este texto na lista de conversas sozinho.

O código correspondente já está no `main`. O que entrou depois do pull neste PC:

- Universo Técnico versão 5: `0fa2059` — aula avulsa com o título da página
- Chip 7 versão 20 e Universo Técnico versão 6: `f7fe30e` — aula avulsa do Chip 7 pelo nome do menu, qualidade, anexos e índice

O registro da manhã (outro PC) continua abaixo. A continuação da noite está em **Continuação neste PC**.

Remote: `https://github.com/Prof-Rob-Tech/PRT-Nexus.git`

## Ambiente neste PC

- Ambiente virtual: `.venv`, Python 3.11.9.
- Ativar no PowerShell: `.\.venv\Scripts\Activate.ps1`
- A política de execução do usuário foi ajustada para `RemoteSigned`, senão o `Activate.ps1` é bloqueado.
- Identidade Git local do projeto: `Prof-Rob-Tech`.
- Pull do dia levou o `main` até `7a3a401` (`universo_mapper.py`, `chip7_view.py`, `universo_view.py`).

## O que foi decidido sobre o projeto

O menu está à frente dos conectores de propósito. Cada conector é feito e testado por vez. O núcleo que funciona é Chip 7 e Universo Técnico.

Feedback da revisão, sem tratar o menu vazio como esquecimento:

- Dos 19 botões da sidebar, 5 abrem tela de verdade: Início, Navegador, Downloads, Universo Técnico e Chip 7.
- YouTube, Biblioteca, Favoritos e Histórico já têm view e ficam de fora da janela.
- Hotmart, Vimeo, Drive, Mega, Configurações, Licença, Atualizações e Plugins são só ícone.
- A Kiwify quebra ao analisar um link: `kiwify_view.py` chama `fetch_course_info`, e `KiwifyConnector` é declarada duas vezes em `kiwify_connector.py`. A segunda apaga a primeira.
- Há sidebar, worker de download e tema duplicados. Chip 7 e Universo repetem CSS.
- Não há `requirements.txt`, teste nem README.

## Universo Técnico

Arquivos: `services/extractors/universo_mapper.py` e `ui/views/universo_view.py`.

O que passou a funcionar e foi testado pelo Robson:

- Login pela página da conta.
- Velocidade no campo amarelo, no formato `2.51MiB/s | ETA: 00:05`. O conector lê a linha do yt-dlp (`at ... ETA ...`). O processo que já estava aberto não atualiza. É preciso fechar e abrir o PRT Nexus.
- Árvore de pastas no padrão do Chip 7. Exemplo conferido dos dois primeiros módulos:

```text
PRT_Nexus/
└── 01 - Nível 1 Curso EAD Consertos de placa iPhone – Eagle Team/
    ├── 01 - Começamos do zero/
    │   ├── 01 - Apresentação do curso.mp4
    │   └── ...
    └── 02 - Softwares para análises/
        ├── 01 - 1.1 ZXW – Imprescindível – Configurações.mp4
        └── ...
```

- A numeração segue a ordem da página e recomeça em cada módulo. O `?` sai do nome do arquivo. A mesma regra vale para os módulos que não estavam na foto.
- Arquivos já baixados na pasta antiga `Universo Técnico - Curso Extraído` não são movidos.
- Pausar segura o yt-dlp e mostra `PAUSADO | ETA: --:--`. Retomar continua. Antes o botão só trocava o texto.
- Cancelar remove da tabela a aula que ficou abaixo de 100%.
- Limpar concluídos apaga as de 100%. Se o download já não está rodando, também apaga a linha interrompida.
- Nome do Conteúdo vira a pasta `01 - nome digitado`.
- Estrutura `Todos os Vídeos na Mesma Pasta` joga todas as aulas nessa pasta, numeradas na ordem do curso inteiro.
- Mídias `Manter Nome Original do Vídeo` grava o título sem o `01 -`.
- `Organizado Automaticamente por Módulo` com `Extração Sequencial` mantém a árvore acima.
- As opções em vermelho (anexos, txt, notificação) ficaram como estavam.

## Chip 7

Arquivos: `services/extractors/chip7_connector.py` e `ui/views/chip7_view.py`.

A velocidade já existia. Foram levadas as mesmas regras do Universo, commit versão 19:

- Pausar mostra `PAUSADO | ETA: --:--` e segura o download até Retomar. Cancelar solta uma pausa.
- Cancelar tira da tabela a aula incompleta.
- Limpar concluídos apaga 100% e, com o download parado, também a linha interrompida.
- Nome do Conteúdo próprio vira `01 - esse nome`. O texto padrão `Chip 7 - Curso Extraído` continua usando o nome do curso na página. Se os dois faltarem, o fallback segue `FACE ID 3.0`.
- `Todos os Vídeos na Mesma Pasta` grava na pasta do curso, com numeração global.
- `Manter Nome Original do Vídeo` tira o prefixo `01 -`.
- `Organizado Automaticamente por Módulo` com `Extração Sequencial` mantém pasta do curso, pasta do módulo e aula numerada.

As combinações de pasta, a pausa e o cancelamento foram testadas fora do site. O download real no Chip 7 ainda precisa do login do Robson, com o PRT Nexus fechado e aberto de novo.

## Continuação neste PC

O outro PC tinha parado no Universo versão 4 e no Chip 7 versão 19. Aqui o `main` foi puxado e o trabalho seguiu nestes pontos, todos testados pelo Robson.

### Universo Técnico — aula avulsa

O botão **Baixar Mídia Avulsa** baixava o vídeo certo com o nome fixo `Aula_Avulsa`. Agora lê o título da página (`h1`) e grava esse nome. Exemplo testado: `7. Pull Down – Nível baixo.mp4`, direto em `Downloads\PRT_Nexus`. O arquivo antigo não é renomeado.

A tabela só tira o prefixo `01 - ` que o conector coloca no curso inteiro. Um título do site como `7. Pull Down` permanece inteiro.

Commit: `0fa2059`.

### Chip 7 — aula avulsa

O link do módulo é único. `https://chip7cursos.com.br/aluno/curso/293` abre sempre a primeira aula. Trocar de aula no site não muda a URL; o menu é um `button.student-lesson-nav__btn`.

Por isso existe o campo **Aula**. O Robson digita o nome como está no menu (`PROTOCOLOS RF`, `SWAP RF`). O conector busca a aula em `/api/student_lessons.php`, clica nesse botão e baixa o vídeo dessa aula, com o título do site, direto em `Downloads\PRT_Nexus`. Sem o nome, o botão avisa e não baixa a primeira de novo.

### Qualidade

A lista (Max, 1080p, 720p, MP3) existia na tela e era ignorada. Agora vale no Chip 7 e no Universo Técnico.

- Max Qualidade continua o melhor MP4.
- 1080p e 720p pedem vídeo até essa altura.
- Apenas Áudio gera MP3.

Se a aula só tiver um arquivo, 1080p e 720p baixam esse arquivo. O áudio extrai o MP3 dele.

### Opções extras do Universo Técnico

O som ao concluir já funcionava. Anexos e o `.txt` estavam só na tela. Agora os dois estão ligados, no mesmo espírito do Chip 7.

- **Anexos** marcado: PDF, ZIP e arquivos parecidos da página vão para a mesma pasta do vídeo. Aula sem material só grava o vídeo.
- **Arquivo .txt** marcado: ao terminar, cria `Downloads\PRT_Nexus\indice_e_descricao_curso.txt` com nome da aula, link e texto da página. Sem descrição, o arquivo avisa isso.

Commit dos três itens acima, junto com a aula avulsa do Chip 7: `f7fe30e`.

## Como levar o chat para outro PC

A conta do Cursor não sincroniza o histórico. Este arquivo é o atalho combinado: `git pull` no outro PC e colar o conteúdo num chat novo.

Export Transcript, no botão direito do chat, gera outro markdown local e não precisa de extensão. Cursor Chat Transfer só seria necessário para a conversa aparecer na lista de chats do outro PC.
